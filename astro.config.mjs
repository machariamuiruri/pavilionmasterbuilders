import { defineConfig } from 'astro/config';

// Static site — outputs plain HTML/CSS/JS to dist/, deploys anywhere (Vercel, Netlify, etc.)
export default defineConfig({
  site: 'https://www.pavilionmasterbuilders.com',
});
